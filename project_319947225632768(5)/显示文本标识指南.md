# 网页显示文本标识指南 📝

## 项目概述
- **项目类型**: React + Vite + TypeScript 个人作品集网站
- **框架**: React 18.2 + TypeScript
- **UI组件**: Radix UI + Tailwind CSS

## 🔴 红色标识 - 直接显示在网页上的文本

### 1. PersonalInfo.tsx 组件中的显示文本

#### 个人信息区域
```tsx
// src/components/PersonalInfo.tsx

// 用户名 (显示在头像下方)
🔴 <h1 className="text-2xl font-bold mb-2 gradient-text smooth-transition">
    John Doe
  </h1>

// 职位标题
🔴 <div className="text-gray-600 text-sm mb-6 font-medium">
    Full Stack Developer
  </div>

// 下载简历按钮文本
🔴 <button className="w-full bg-gradient-to-r from-blue-500 to-purple-600 text-white py-3 px-6 rounded-xl font-medium hover-lift ripple-effect smooth-transition hover:shadow-lg">
    <Download className="w-4 h-4 mr-2 inline-block" />
    Download Resume
  </button>
```

#### 社交链接标签 (这些通常不显示，但可能在tooltip中显示)
```tsx
🔴 {[
    { icon: Github, label: '🔴 GitHub' },
    { icon: Linkedin, label: '🔴 LinkedIn' },
    { icon: Mail, label: '🔴 Email' },
    { icon: Globe, label: '🔴 Website' }
]}
```

#### 关于我部分
```tsx
// 章节标题
🔴 <h2 className="text-2xl font-bold text-gray-800">About Me</h2>

// 个人介绍文本
🔴 <p className={`${mounted ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-4'} smooth-transition`}
     style={{ animationDelay: '0.5s', animation: mounted ? 'text-fade-in-up 0.6s ease-out 0.5s both' : 'none' }}>
    Passionate full-stack developer with expertise in creating immersive digital experiences that bridge the gap between technology and creativity.
  </p>
🔴 <p className={`${mounted ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-4'} smooth-transition`}
     style={{ animationDelay: '0.6s', animation: mounted ? 'text-fade-in-up 0.6s ease-out 0.6s both' : 'none' }}>
    Specializing in interactive media, modern web technologies, and innovative design patterns. I transform ideas into engaging digital realities through clean code and thoughtful user experiences.
  </p>
```

#### 技能部分
```tsx
// 章节标题
🔴 <h2 className="text-2xl font-bold text-gray-800">Skills & Expertise</h2>

// 技能名称列表
🔴 {[
    { name: '🔴 JavaScript/TypeScript', color: 'from-yellow-400 to-orange-500' },
    { name: '🔴 React/Vue', color: 'from-blue-400 to-cyan-500' },
    { name: '🔴 Node.js/Python', color: 'from-green-400 to-emerald-500' },
    { name: '🔴 UI/UX Design', color: 'from-purple-400 to-pink-500' },
    { name: '🔴 Database Design', color: 'from-indigo-400 to-purple-500' },
    { name: '🔴 Cloud Services', color: 'from-sky-400 to-blue-500' }
]}
```

#### 导航按钮
```tsx
// About按钮
🔴 <button
    className="px-8 py-3 bg-white/60 backdrop-blur-sm text-gray-700 rounded-xl font-medium hover-lift smooth-transition ripple-effect border border-white/40"
    onClick={() => onSectionChange('about')}
  >
    <User className="w-4 h-4 mr-2 inline-block" />
    About
  </button>

// Portfolio按钮
🔴 <button
    className="px-8 py-3 bg-gradient-to-r from-blue-500 to-purple-600 text-white rounded-xl font-medium hover-lift smooth-transition ripple-effect"
    onClick={() => onSectionChange('portfolio')}
  >
    <Sparkles className="w-4 h-4 mr-2 inline-block" />
    Portfolio
  </button>
```

### 2. Portfolio.tsx 组件中的显示文本

#### 头部导航
```tsx
// 返回按钮
🔴 <button
    className="px-6 py-3 bg-white/60 backdrop-blur-sm text-gray-700 rounded-xl font-medium hover-lift smooth-transition ripple-effect border border-white/40"
    onClick={onBack}
  >
    <ArrowLeft className="w-4 h-4 mr-2 inline-block" />
    Back
  </button>

// 页面标题
🔴 <h1 className="text-4xl font-bold gradient-text">Portfolio</h1>
```

#### 统计卡片
```tsx
🔴 {[
    { number: '🔴 37', label: '🔴 Total Projects', icon: FileText, color: 'from-blue-400 to-cyan-500' },
    { number: '🔴 3', label: '🔴 Categories', icon: TrendingUp, color: 'from-purple-400 to-pink-500' },
    { number: '🔴 12', label: '🔴 Awards', icon: Award, color: 'from-yellow-400 to-orange-500' },
    { number: '🔴 284K', label: '🔴 Total Views', icon: Eye, color: 'from-green-400 to-emerald-500' }
]}
```

#### 标签页导航
```tsx
// Literary标签
🔴 <TabsTrigger
    value="literary"
    className="data-[state=active]:bg-white data-[state=active]:shadow-lg text-gray-700 font-medium rounded-xl smooth-transition ripple-effect hover:bg-white/50"
  >
    <FileText className="w-4 h-4 mr-2" />
    Literary
  </TabsTrigger>

// Video标签
🔴 <TabsTrigger
    value="video"
    className="data-[state=active]:bg-white data-[state=active]:shadow-lg text-gray-700 font-medium rounded-xl smooth-transition ripple-effect hover:bg-white/50"
  >
    <Video className="w-4 h-4 mr-2" />
    Video
  </TabsTrigger>

// Games标签
🔴 <TabsTrigger
    value="games"
    className="data-[state=active]:bg-white data-[state=active]:shadow-lg text-gray-700 font-medium rounded-xl smooth-transition ripple-effect hover:bg-white/50"
  >
    <GamepadIcon className="w-4 h-4 mr-2" />
    Games
  </TabsTrigger>
```

#### 页脚信息
```tsx
🔴 <span className="text-sm">
    Total projects: 37 | Last updated: {new Date().toLocaleDateString()}
  </span>
```

## 🟡 黄色标识 - 动态生成的文本

这些文本会根据状态或时间动态变化：

```tsx
// 动态日期
🟡 {new Date().toLocaleDateString()}

// 组件状态文本 (通常不显示，但可能在开发调试时使用)
🟡 {activeTab === 'literary' ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-4'}
```

## ⚪ 白色标识 - 不显示的文本

这些是注释、变量名、CSS类名等，不会直接显示在网页上：

```tsx
// CSS类名
⚪ className="text-2xl font-bold mb-2 gradient-text smooth-transition"

// 变量名
⚪ const [mounted, setMounted] = useState(false);

// 属性名
⚪ onClick={() => onSectionChange('portfolio')}

// 注释
⚪ {/* 水波纹背景 */}
```

## 📋 文本修改优先级

### 🔥 高优先级 (用户直接看到的)
1. **个人信息**: `John Doe`, `Full Stack Developer`
2. **按钮文本**: `Download Resume`, `About`, `Portfolio`, `Back`
3. **页面标题**: `About Me`, `Skills & Expertise`, `Portfolio`
4. **技能列表**: `JavaScript/TypeScript`, `React/Vue` 等
5. **统计数据**: `37`, `Total Projects` 等

### 🔸 中优先级 (功能性文本)
1. **标签页名称**: `Literary`, `Video`, `Games`
2. **社交标签**: `GitHub`, `LinkedIn`, `Email`, `Website`

### 🔹 低优先级 (动态或辅助文本)
1. **页脚信息**: 包含动态日期的项目统计
2. **aria-labels 和 accessibility 文本**

## 🛠️ 修改建议

### 1. 个人信息修改
```tsx
// 修改姓名和职位
🔴 <h1 className="text-2xl font-bold mb-2 gradient-text smooth-transition">
    [你的新名字]
  </h1>

🔴 <div className="text-gray-600 text-sm mb-6 font-medium">
    [你的新职位]
  </div>
```

### 2. 按钮文本修改
```tsx
// 修改下载简历按钮
🔴 <button ...>
    <Download className="w-4 h-4 mr-2 inline-block" />
    [新按钮文本]
  </button>
```

### 3. 技能列表修改
```tsx
🔴 {[
    { name: '🔴 [新技能1]', color: 'from-yellow-400 to-orange-500' },
    { name: '🔴 [新技能2]', color: 'from-blue-400 to-cyan-500' },
    // ...更多技能
]}
```

## ⚠️ 注意事项

1. **保持CSS类名不变** - 只修改显示文本内容
2. **保持图标与文本的配对** - 如 `Download` 图标与 `Download Resume` 文本
3. **测试响应式布局** - 修改文本长度后检查在不同屏幕尺寸下的显示效果
4. **保持多语言一致性** - 如果考虑国际化，确保所有文本都用同一语言
5. **避免特殊字符** - 某些特殊字符可能导致显示问题

## ✅ 验证方法

修改完成后请检查：
- [ ] 所有标识的红色文本都已正确修改
- [ ] 按钮功能正常工作
- [ ] 页面布局没有因为文本长度变化而破坏
- [ ] TypeScript编译没有错误
- [ ] 在不同设备上显示正常